namespace Common.Utilities
{
    public enum Visibility
    {
        Private,
        Public,
        OnlyFriends
    }

    public enum PlayingStatus
    {
        Abandoned,
        Paused,
        Playing,
        Beaten
    }
}
